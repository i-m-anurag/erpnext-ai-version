{
  "module": "auth",
  "permissions": [
    { "action": "user.read", "description": "View users" },
    { "action": "user.create", "description": "Create users / send welcome" },
    { "action": "user.update", "description": "Update user profile/status" },
    { "action": "user.disable", "description": "Disable a user and revoke sessions" }
  ]
}
